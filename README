This is a simple HTTP proxy for geocoder providers. Used by otp.js user interface. So far this just supports Yahoo! BOSS Geo services (which necessitate a server-side component to support OAuth 1.0 request signing). Additional providers can be proxied in order to simplify/unify API specifications. 
